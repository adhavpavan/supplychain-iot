name: Caliper Benchmarks
version: "2.0.0"
# mutual-tls: false

caliper:
  blockchain: fabric

info:
  Version: 2.5.9
  Size: 3 Orgs with 1 Peer
  Orderer: Raft
  Distribution: Single Host
  StateDB: CouchDB
 
channels:
  - channelName: mychannel
    # the chaincodeIDs of all the fabric chaincodes in caliper-benchmarks
    contracts:
    - id: performance-go
    - id: performance

organizations:
  - mspid: Org1MSP
    # Identities come from cryptogen created material for test-network
    identities:
      certificates:
      - name: 'User1'
        clientPrivateKey:
          path: ../../../artifacts/channel/crypto-config/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/keystore/priv_sk
        clientSignedCert:
          path: ../../../artifacts/channel/crypto-config/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/signcerts/cert.pem
    connectionProfile:
      path: ../../../artifacts/channel/crypto-config/peerOrganizations/org1.example.com/connection-org1.json
      discover: true
